# ============================================
# Configuration Docker - Variables d'environnement
# ============================================
# 
# Ce fichier contient les variables utilisées par docker-compose.yml
# pour configurer les services Docker.
#
# IMPORTANT: NE PAS COMMITTER EN PRODUCTION avec ces valeurs par défaut
# Ce fichier est inclus dans .gitignore pour éviter les fuites de credentials
# ============================================

# ==========================================
# PostgreSQL Configuration
# ==========================================
# Base de données utilisée par Symfony via Doctrine ORM
# Ces valeurs sont injectées dans le service 'db' de docker-compose.yml
POSTGRES_DB=tickets
POSTGRES_USER=symfony
POSTGRES_PASSWORD=symfony

# Note: En production, utiliser des credentials forts et un service géré
# (ex: AWS RDS, Azure Database for PostgreSQL, Google Cloud SQL)

# ==========================================
# Ports exposés (définis dans docker-compose.yml)
# ==========================================
# Décommentez pour personnaliser les ports si nécessaire
# BACKEND_PORT=8000
# FRONTEND_PORT=5173
# POSTGRES_PORT=5433
# FRONTEND_PORT=5173
# DB_PORT=5432
